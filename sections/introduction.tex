\chapter{Introduction}

% \myx{Some general suggestions for best efficiency: (1) please use a different color (by defining a macro) to mark what you have changed since our last review, (2) for comments that need nontrivial changes, please leave a note and keep the comments after addressing it. The person who left that comment will delete it to make sure the problem is truly resolved, (3) feel free to use AI tools to polish the text, but do not directly generate text with AI.}

% \frank{(1) There's been substantial changes since the last review and I can't recall all exact locations, but most changes are w/ evaluation sections like \ref{sec:ch1_eval} and Chapter \ref{cha:ch3}. Will mark future changes with {\colorGreen this color} onwards.}

% \myx{It seems that currently there are lots of tex compilation warnings. Please fix them to make sure all refs are correct.} \frank{$\leftarrow$ Resolved}
\mytodo{a bit more background on agents?} 
% \myx{Yes, I think we might need a sentence between the first and second sentence below giving a few examples about agents for coding. For example, you can list Gemini Cli, Colab AI, Claude Code, etc..}

Recent studies on AI coding agents have proven the efficacy of using Large Language Models (LLMs) to handle coding tasks such as code generation and debugging. To apply LLMs in daily software engineering scenarios, both industry and academia have proposed many agentic frameworks. Industry-proprietary examples of agentic frameworks include Google's Colab AI \cite{google_2025_colab_ai_first}, Claude Code \cite{anthropic_2025_claude_code_ga}, and Cursor Agent \cite{cursor_2025_agent_cli}. {\colorGreen There is a continuous and rapid growth of user bases for the above frameworks, reflecting their perceived utility in real-world software engineering tasks. This implies increasing demand for production-ready AI assistants that integrate seamlessly into existing development workflows.}
% \myx{list some successful applications here.} \myx{Add something like "To apply LLMs in the coding workflow, prior research has proposed several agentic frameworks, such as ..."}
Besides those proprietorial frameworks, there are also many open source implementations from both academia and industry. Examples of these open-source agentic frameworks include
% Some open-source agentic frameworks developed over the past few years include 
Gemini Cli \cite{mullen_salva_2025_gemini_cli}, SWE-Agent \cite{yang2024sweagent}, AutoCodeRover \cite{zhang2024autocoderover}, Agentless \cite{xia2024agentlessdemystifyingllmbasedsoftware}, etc. 
% \myx{The logic here is a little bit weird, maybe a better way is: "These frameworks enable existing LLMs to achieve ...", also, maybe this should not start a new paragraph.}
These frameworks enable existing LLMs to achieve human-level performance when prompted with clear instructions and sufficiently rich context.
% All of such frameworks are good examples of LLMs being capable of achieving human-level performance when conditions are met \myx{what conditions? need to be specific}. 
Despite these advances, current agentic systems still face two major challenges
%\myx{It is not a "Challenge". We don't want to attack existing methods here. Instead, you should refer to it as challenges. Something like "there are two key challenges when designing agentic serving frameworks", need to add some more text for transition between the sentences to make it logically smooth}
: 1. How to provide accurate and complete context information to the LLM for the best performance / code quality (later referred to as \textbf{Challenge 1}) 
% \myx{Phrase it as a challenge. e.g.: Challenge 1: How to provide correct context information to the LLM for best coding performance?}
\cite{ouyang2025repographenhancingaisoftware}; 2. How to ensure fast patch generation when the LLM is fed with a large number of requests and/or large number of tokens during codebase exploration (later referred to as \textbf{Challenge 2}). 
% \mytodo{give detailed examples give} \myx{No, we don't need examples here as each one of them is solved by prior work. We need examples for joint optimization below.}

More recently, there have been works that address either Challenge 1 or Challenge 2\myx{Briefly describes what existing work does. I assume there will be a few papers that address challenge 1 with some context retrial algorithm, and challenge 2 with prefix caching} \frank{There are many papers relevant to just repository level context retrieval, but they are for inline code completion/copilot instead of agents. Should I include those and say they are relevant?}\myx{Yes, but at a very high level, i.e. a few words / a short sentence}. However, no existing system attempts to tackle both challenges at once. \myx{Need to motivate here why we want to perform a joint optimization. Show with some examples.} There are many reasons why a joint optimization is highly desirable, the most apparent being cost. \mytodo{find relevant examples, I think Swebench/sweagent paper has data on cost per issue}. For a complex code repository and an intricate issue\myx{"issue" is not defined here, maybe we can say "coding task" instead? In general, when you use a term, you need to first define it, or you can just use plain language to convey the idea and formally introduce the term later on}, it often takes a significantly long time for the agent to generate a patch, even when the patch is unlikely to resolve the issue given the complexity. And if the patch is deemed problematic, the consumed time and token length all contribute to the cumulative engineering expense. \myx{How is this related to why people don't do joint optimization?}
% This is due to the difficulty of balancing performance, cost, and latency trade-offs. \myx{Is this really the reason? Actually, we don't need a reason for why they did not do this.} \frank{There is no proof, just my hypothesis, commented out}

This thesis project aims to fill the gap in understanding: what are the solutions used by existing frameworks to address the challenges, how these solutions affect the performance, 
% the performance improvement brought by applying corresponding state-of-the-art solutions for each challenge, 
and what trade-offs need to be considered for joint optimization of context retrieval and patch generation latency. Our results and analysis illustrate the possibility as well as difficulty of integrating such solutions to step towards a fast yet performant code LLM-based agentic framework. A quick road map of this paper is as follows \myx{"The paper is organized as follows:"}:

% This thesis project aims to fill the gap in understanding: how each challenge is present in existing frameworks \myx{something like "what are the solutions used by existing frameworks to address the challenges, and how does these solutions (or lack of them) affect the performance"}, the nature of the conflicting trade-off between the two Challenges \myx{"what are the tradeoffs we need to consider when performing jointly optimization of xxx and xxx" (maybe this should be the thrid point?)}, and the advantages of corresponding state-of-the-art solutions for each Challenge \myx{something like "the performance improvement brought by applying ..."}. \mrem{This project also discusses} \madd{Our results and analysis illustrate} the possibility as well as difficulty of integrating such solutions to step towards a fast yet performant code LLM-based agentic framework. A quick road map of this paper is as follows:

\mytodo{update below once restructured}
\vspace{0.1in}
\begin{itemize}
    \item In Chapter~\ref{cha:ch1}, we inspect the presence of Challenge 1 \myx{use a short phase to describe the challenge, and put Challenge 1 in parenthesis.} in existing systems, then evaluate the performance of RepoGraph \cite{ouyang2025repographenhancingaisoftware}, a state-of-the-art solution \myx{Maybe refer to it as "context retrieval algorithm that addresses Challenge 1"?} for Challenge 1. The evaluation primarily concerns consistency of performance using different model backends.
    \item In Chapter~\ref{cha:ch2}, we inspect the presence of Challenge 2 \myx{same as above, point out the challenge with a short phrase} in existing systems, evaluate the latency of CacheBlend \cite{yao2025cacheblendfastlargelanguage}, a state-of-the-art solution targeting Challenge 2 \myx{same as above, "caching algorithm" instead of "solution"}. The evaluation involves a comparison with legacy systems such as vLLM and SGLang \cite{kwon2023vllm, zheng2024sglangefficientexecutionstructured}. \myx{For the following sentence, maybe we don't need to explicitly point out the fact that our setup is different from what is used in vLLM and SGLang paper. Just say "For our evaluations, we focus on ...".} The primary distinction of our setup from that of the authors is that this evaluation uses RepoGraph produced agent prompts as dataset, and is meant to measure latency under real-world code agent use case scenarios. 
    \item In Chapter~\ref{cha:ch3}, we discuss how the above solutions can be integrated into a unified end-to-end code agent system that is reliable, fast, and provides high-quality code generation results. 
\end{itemize}
